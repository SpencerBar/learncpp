



/*#include <iostream>
#include <vector>
#include <algorithm>
#include <iterator>

double max(double x, double y)
{
	if (x > y)
		return x;
	else
		return y;
}

void swap(int& x, int& y)
{
	int temp{ x };
	x = y;
	y = temp;
}

int& getLargestNum(std::vector<int>& array, int length)
{
 return *std::max_element(array.begin(), array.end());

}

int main()
{
	std::vector<int> array{ 2, 3, 4, 5, 6, 7, 8, 9, 10 };
	
	int& ptrmx{ getLargestNum(array,array.size()) };
	
	return 0;
}*/

